# SIEM Analyzer Production Environment Template
# Rename this to .env on your production server

# Database Settings
DB_USER=admin
DB_PASSWORD=your_secure_password_here
DB_NAME=siem_analyzer

# Redis Settings
# Since Redis is already on the server, point to it.
# If using Docker bridge network, use the host IP or 'host.docker.internal'
REDIS_URL=redis://host.docker.internal:6379/0

# Security
ADMIN_API_KEY=your_secure_admin_key_here
ALLOWED_ORIGINS=https://your-tunnel-hostname.com,https://your-frontend-domain.com

# Analytics
MULTI_TENANT_ENABLED=true
LOG_LEVEL=INFO
